{% extends 'base.html.twig' %}
{% block body %}
<nav class="fixed top-0 left-64 z-50 w-full">
    <div class="px-3 py-3 lg:px-5 lg:pl-3 flex justify-center">
{#            <span class="l-40" >Company</span>#}

    </div>
</nav>

<aside id="logo-sidebar" class="fixed top-0 left-0 z-40 w-64 h-screen transition-transform -translate-x-full sm:translate-x-0" aria-label="Sidebar">
    <div class="flex flex-col h-full px-3 pb-4 ">
        <div class="top-0">
            <button data-drawer-target="logo-sidebar" data-drawer-toggle="logo-sidebar" aria-controls="logo-sidebar" type="button" class="inline-flex items-center p-2 text-sm text-gray-500 rounded-lg sm:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600">
                <svg class="w-6 h-6" aria-hidden="true" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
                    <path clip-rule="evenodd" fill-rule="evenodd" d="M2 4.75A.75.75 0 012.75 4h14.5a.75.75 0 010 1.5H2.75A.75.75 0 012 4.75zm0 10.5a.75.75 0 01.75-.75h7.5a.75.75 0 010 1.5h-7.5a.75.75 0 01-.75-.75zM2 10a.75.75 0 01.75-.75h14.5a.75.75 0 010 1.5H2.75A.75.75 0 012 10z"></path>
                </svg>
            </button>
            <a href="{{path('front_default_index')}}" class="flex ml-2">
                <img src="{{app.request.baseURL}}/uploads/images/global/logo.svg" class="h-16 mr-3" alt="Logo" />
            </a>
        </div>

        <ul class="font-medium mt-12 ml-4">
            <li>
                <a href="{{path('back_default_index')}}" class="flex items-start space-x-2 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                   <img src="{{app.request.baseURL}}/uploads/images/global/dashboard.svg" class="ml-1 h-6" alt="Dashboard" />
                    <span>{{ 'dashboard'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% if is_granted('ROLE_CUSTOMER') and not is_granted('ROLE_ACCOUNTANT') or is_granted('ROLE_ADMIN') %}
            <li>
                <a href="{{path('back_app_estimate_index')}}" class="flex items-start space-x-2 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/estimate.svg" class="h-6" alt="Estimate"/>
                    <span>{{ 'estimate'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% endif %}
            <li>
                <a href="{{path('back_app_invoice_index')}}" class="flex items-start space-x-2.5 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/invoice.svg" class="ml-1 h-6" alt="Invoice" />
                    <span>{{ 'invoice'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% if is_granted('ROLE_MECHANIC') %}
            <li>
                <a href="{{path('back_app_customer_index')}}" class="flex items-start space-x-4 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/customer.svg" class="ml-1 h-6" alt="Customer" />
                    <span>{{ 'customer'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_CUSTOMER') and not is_granted('ROLE_ACCOUNTANT') or is_granted('ROLE_ADMIN') %}
            <li>
                <a href="{{path('back_app_prestation_index')}}" class="flex items-start p-2 space-x-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/prestation.svg" class="ml-1 h-6" alt="Prestation" />
                    <span>{{ 'prestation'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_MECHANIC') %}
            <li>
                <a href="{{path('back_app_category_index')}}" class="flex items-start space-x-3 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/category.svg" class="ml-1 h-6" alt="Product" />
                    <span>{{ 'category'|trans({}, 'default') }}</span>
                </a>
            </li>
            <li>
                <a href="{{path('back_app_product_index')}}" class="flex items-start space-x-3 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/product.svg" class="ml-1 h-6" alt="Product" />
                    <span>{{ 'product'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ACCOUNTANT') %}
            <li>
                <a href="{{path('back_app_accounting_index')}}" class="flex items-start space-x-3 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/accounting.svg" class="ml-1 h-6" alt="Product" />
                    <span>{{ 'accounting'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
            <li>
                <a href="{{path('back_app_user_index')}}" class="flex items-start space-x-4 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/customer.svg" class="ml-1 h-6" alt="User" />
                    <span>{{ 'user'|trans({}, 'default') }}</span>
                </a>
            </li>
            {% endif %}
        </ul>
        <ul class="font-medium top-[800px] absolute ml-4">
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a href="{{ path('back_app_company_show') }}" class="flex items-start space-x-2 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                        <img src="{{app.request.baseURL}}/uploads/images/global/site.svg" class="h-6" alt="Account" />
                        <span>{{ 'my site'|trans({}, 'default') }}</span>
                    </a>
                </li>
            {% endif %}
            <li>
                <a href="{{ path('back_app_account_show') }}" class="flex items-start space-x-2 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/account.svg" class="h-6" alt="Account" />
                    <span>{{ 'my account'|trans({}, 'default') }}</span>
                </a>
            </li>
            <li>
                <a href="{{path('app_logout')}}" class="flex items-start space-x-2 p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-700">
                    <img src="{{app.request.baseURL}}/uploads/images/global/logout.svg" class="h-6" alt="Log Out" />
                    <span>{{ 'log out'|trans({}, 'default') }}</span>
                </a>
            </li>
        </ul>
    </div>
</aside>

<main class="p-4 sm:ml-64 mt-14 h-full">
    {% for label, messages in app.flashes %}
    {% for message in messages %}
        {% if label == 'success' %}
            <div id="flash-message" class="flex items-center p-4 mb-4 text-sm text-green-800 rounded-lg bg-green-50 dark:bg-gray-800 dark:text-green-400" role="alert">
        {% elseif label == 'error' %}
            <div id="flash-message" class="flex items-center p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50 dark:bg-gray-800 dark:text-red-400" role="alert">
        {% elseif label == 'warning' %}
            <div id="flash-message" class="flex items-center p-4 mb-4 text-sm text-yellow-800 rounded-lg bg-yellow-50 dark:bg-gray-800 dark:text-yellow-400" role="alert">
        {% elseif label == 'info' %}
            <div id="flash-message" class="flex items-center p-4 mb-4 text-sm text-blue-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert">
                    {% endif %}
                    <svg class="flex-shrink-0 inline w-4 h-4 mr-3" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                        <path d="M10 .5a9.5 9.5 0 1 0 9.5 9.5A9.51 9.51 0 0 0 10 .5ZM9.5 4a1.5 1.5 0 1 1 0 3 1.5 1.5 0 0 1 0-3ZM12 15H8a1 1 0 0 1 0-2h1v-3H8a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1v4h1a1 1 0 0 1 0 2Z"/>
                    </svg>
                    <span class="sr-only">Info</span>
                    <div>
                        <span class="font-medium">{{ label }}</span> {{ message }}
                    </div>
                </div>
                {% endfor %}
                {% endfor %}
    <div class="p-4 dark:border-gray-700 mt-14 container mx-auto">
        {% block main %}
        {% endblock %}
    </div>
</main>
{% endblock %}