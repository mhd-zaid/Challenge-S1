<form method="post" action="{{ path('back_app_company_delete', {'id': company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
    <button class="button button-invalid px-4 flex items-center justify-center space-x-4">Supprimer</button>
</form>
