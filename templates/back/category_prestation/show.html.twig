{% extends 'base.html.twig' %}

{% block title %}CategoryPrestation{% endblock %}

{% block body %}
    <h1>CategoryPrestation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_prestation.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ category_prestation.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ category_prestation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_app_category_prestation_index') }}">back to list</a>

    <a href="{{ path('back_app_category_prestation_edit', {'id': category_prestation.id}) }}">edit</a>

    {{ include('back/category_prestation/_delete_form.html.twig') }}
{% endblock %}
