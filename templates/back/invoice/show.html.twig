{% extends 'back/base.html.twig' %}

{% block title %}Invoice{% endblock %}

{% block main %}
    <h1>Invoice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('back_app_invoice_index') }}">back to list</a>

    <a href="{{ path('back_app_invoice_edit', {'id': invoice.id}) }}">edit</a>

    {{ include('back/invoice/_delete_form.html.twig') }}
{% endblock %}
