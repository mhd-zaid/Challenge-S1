{% extends 'back/base.html.twig' %}

{% block title %}{{ 'edit invoice'|trans({}, 'invoice') }}{% endblock %}

{% block body %}
    <h1>{{ 'edit invoice'|trans({}, 'invoice') }}</h1>

    {{ include('back/invoice/_form.html.twig', {'button_label': 'update'}) }}

    <a href="{{ path('back_app_invoice_index') }}">{{ 'back to list'|trans({}, 'invoice') }}</a>

    {{ include('back/invoice/_delete_form.html.twig') }}
{% endblock %}
