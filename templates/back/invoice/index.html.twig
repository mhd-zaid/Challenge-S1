{% extends 'back/base.html.twig' %}

{% block title %}Invoice index{% endblock %}

{% block main %}
    <h1>Invoice index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for invoice in invoices %}
            <tr>
                <td>{{ invoice.id }}</td>
                <td>
                    {# <a href="{{ path('back_app_invoice_show', {'id': invoice.id}) }}">show</a> #}
                    {# <a href="{{ path('back_app_invoice_edit', {'id': invoice.id}) }}">edit</a> #}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {# <a href="{{ path('back_app_invoice_new') }}">Create new</a> #}
{% endblock %}
