<div class="flex flex-col items-center">
    <h1>Mot de passe oublié ?</h1>
    <p>Saisissez votre email pour recevoir un lien de réinitiation</p>
</div>
<div class="mt-4 w-[406px]">
    {{ form_start(form) }}
    <div class="flex space-x-4">
        {{ form_label(form.email, 'Email', {'label_attr': {'class': 'flex justify-center items-center mb-0'}} ) }}
        {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
    </div>
    <div class="mt-4 w-full">
        <button class="button button-details w-full">{{ button_label|default('Envoyer') }}</button>
        {{ form_end(form) }}
    </div>
    <div class="mt-4 w-full">
        <a href="{{ path('app_login') }}" class="button-details w-full">
            <button class="button button-details w-full">{{ button_label|default('Se connecter') }}</button>
        </a>
    </div>
</div>